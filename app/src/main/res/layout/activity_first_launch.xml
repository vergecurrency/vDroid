<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout_first_launch"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:animateLayoutChanges="true"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/fl_welcome_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/bg_main" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/fl_welcome_verge_logo"
            style="@style/splash_verge_logo" />

        <TextView
            android:text="@string/splash_title"
            style="@style/splash_verge_title" />

        <TextView
            android:id="@+id/launch_verge_sub"
            android:text="@string/first_launch_description"
            style="@style/splash_verge_subtitle" />


    </LinearLayout>

    <RelativeLayout
        android:id="@+id/first_launch_buttons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"

        >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentBottom="true"
            android:orientation="vertical">

            <Button
                android:id="@+id/button_create_wallet"
                android:text="@string/prog_placeholder"
                style="@style/splash_verge_button.up"
                />

            <Button
                android:id="@+id/button_restore_wallet"
                android:text="@string/prog_placeholder"
                style="@style/splash_verge_button.down"/>

        </LinearLayout>
    </RelativeLayout>


</RelativeLayout>